import React, { useState, useContext } from "react";
import campgroundsState from "../context/campgrounds/CampgroundsState";
import CampgroundsContext from "../context/campgrounds/campgroundsContext";

const Campgrounds = () => {
  const campgroundsContext = useContext(CampgroundsContext);

  return <div></div>;
};

export default Campgrounds;
